{
  "expo": {
    "name": "YAPILAPI",
    "slug": "yapilapi",
    "scheme": "yapilapi",
    "version": "0.1.0",
    "orientation": "portrait",
    "userInterfaceStyle": "automatic",
    "ios": {
      "bundleIdentifier": "com.yapilapi.app",
      "supportsTablet": true,
      "infoPlist": {
        "NSCameraUsageDescription": "YAPILAPI uses the camera for video calls and to capture Reals.",
        "NSMicrophoneUsageDescription": "YAPILAPI uses the microphone for audio and video calls and to record voice messages.",
        "UIBackgroundModes": ["audio"],
        "NSContactsUsageDescription": "YAPILAPI uses your contacts to show which of your friends are already here. Emails and numbers are turned into codes on your phone before anything is sent, and nothing is kept."
      }
    },
    "android": {
      "package": "com.yapilapi.app",
      "permissions": [
        "android.permission.CAMERA",
        "android.permission.RECORD_AUDIO",
        "android.permission.MODIFY_AUDIO_SETTINGS",
        "android.permission.ACCESS_NETWORK_STATE",
        "android.permission.BLUETOOTH_CONNECT",
        "android.permission.WAKE_LOCK",
        "android.permission.READ_CONTACTS"
      ],
      "blockedPermissions": ["android.permission.WRITE_CONTACTS"]
    },
    "plugins": [
      "expo-router",
      "expo-secure-store",
      [
        "expo-camera",
        {
          "cameraPermission": "YAPILAPI uses the camera for video calls and to capture Reals.",
          "microphonePermission": "YAPILAPI uses the microphone for audio and video calls and to record voice messages.",
          "recordAudioAndroid": true
        }
      ],
      "expo-notifications",
      "expo-status-bar",
      [
        "@config-plugins/react-native-webrtc",
        {
          "cameraPermission": "YAPILAPI uses the camera for video calls and to capture Reals.",
          "microphonePermission": "YAPILAPI uses the microphone for audio and video calls and to record voice messages."
        }
      ],
      "expo-dev-client",
      "expo-font",
      [
        "expo-localization",
        {
          "supportedLocales": ["en", "fr", "ar", "es", "pt", "sw", "yo", "ha"]
        }
      ],
      "expo-video",
      [
        "expo-image-picker",
        {
          "photosPermission": "YAPILAPI uses your photos and videos so you can share them in posts, reels and stories.",
          "cameraPermission": "YAPILAPI uses the camera for video calls and to capture Reals.",
          "microphonePermission": "YAPILAPI uses the microphone for audio and video calls and to record voice messages."
        }
      ],
      [
        "expo-audio",
        {
          "microphonePermission": "YAPILAPI uses the microphone for audio and video calls and to record voice messages.",
          "recordAudioAndroid": true
        }
      ],
      "expo-sharing",
      [
        "expo-contacts",
        {
          "contactsPermission": "YAPILAPI uses your contacts to show which of your friends are already here. Emails and numbers are turned into codes on your phone before anything is sent, and nothing is kept."
        }
      ]
    ],
    "extra": {
      "apiUrl": "http://localhost:4000",
      "webUrl": "http://localhost:3000",
      "eas": {
        "projectId": ""
      }
    }
  }
}
